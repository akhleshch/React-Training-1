import React from 'react';
import Select  from 'react-select';
import { list } from './mock/mock-list';

const CountryList= (onContryChanged) => {    

    const check = () =>{
        // console.log("Its working---------------");
}
//onChange={(date) => setStartDate(date)
    return (
        <div>
        </div>
    );
}


export default CountryList;